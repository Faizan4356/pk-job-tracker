# Minimal dependencies for the daily automated pipeline (pipeline.py:
# scrape -> store -> AI-extract -> Telegram alert). Deliberately excludes
# streamlit (dashboard.py), pdfplumber (pdf_extract.py), and
# sentence-transformers (semantic_match.py, pulls in torch) — none of those
# run in the daily GitHub Actions workflow, so installing them there would
# just waste CI time/bandwidth every day. See requirements.txt for the full
# set needed for local development.

requests>=2.31
beautifulsoup4>=4.12
google-genai>=0.3
python-dotenv>=1.0
